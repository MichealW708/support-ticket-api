services:
  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      Security__SupportAgentKey: dev-support-key
      Security__AdminKey: dev-admin-key
